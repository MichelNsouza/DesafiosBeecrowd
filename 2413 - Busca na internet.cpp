#include <stdio.h>

int main() {
    
    int numlink3, numlink2, numlink1;
    
    scanf("%d", &numlink3);
    
    numlink2 = numlink3*2;
    numlink1 = numlink2*2;
    
    printf("%d\n", numlink1);
    
    return 0;
}

